import React from 'react';

const ActiveRoomButton = ({
                              creatorUsername,
                              roomId,
                              amountOfParticipants,
                              isUserInRoom,
                          }) => {
    return (
        <div>

        </div>
    );
};

export default ActiveRoomButton;
