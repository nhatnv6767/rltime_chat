import React from 'react';
import AuthBox from "../../shared/components/AuthBox";

const LoginPage = () => {
    return (
        <AuthBox>
            LoginPage login
        </AuthBox>
    );
};

export default LoginPage;
